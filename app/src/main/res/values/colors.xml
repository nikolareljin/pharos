<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Mirrors ui/theme/Color.kt. The adaptive icon background and the app
         window background have to be resources, not Compose values, because the
         launcher and the window manager draw them before any Compose code runs. -->
    <color name="pharos_slate_900">#FF0F172A</color>
    <color name="ic_launcher_background">#FF0F172A</color>
</resources>

# No app-specific rules yet. R8 is enabled for release; anything the
# protocol layer needs to keep (serializer names, reflective lookups) gets a
# rule here with a comment saying what breaks without it.
